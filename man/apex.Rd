% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/runAPEX.R
\name{apex}
\alias{apex}
\title{APEX (Associating Plasma Epigenomics with eXpression), framework to predict gene expression from plasma epigenomics}
\usage{
apex(
  frag_file_k4,
  frag_file_k36 = NULL,
  frag_file_k27 = NULL,
  upstream = 3000,
  downstream = 3000,
  custom_coordinates = NULL,
  num_of_promoter_tiles = 20L,
  num_of_genebody_tiles = 20L,
  num_of_intergenic_tiles = 1L,
  num_of_intragenic_tiles = 1L,
  fastMode = TRUE,
  useAltPromoter = TRUE,
  minNorm = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{frag_file_k4}{Optional path to an H3K4me3 fragment file.
H3K4me3 is recommended and, when available, is used for alternate-promoter
selection. At least one supported histone-mark fragment file must be supplied.}

\item{frag_file_k36}{Path to the H3K36me3 fragment file (optional).}

\item{frag_file_k27}{Path to the H3K27ac fragment file (optional).}

\item{upstream}{Integer. Number of base pairs upstream to include in promoter regions. Default: 3000.}

\item{downstream}{Integer. Number of base pairs downstream to include in promoter regions. Default: 3000.}

\item{custom_coordinates}{Optional path to a BED file containing
user-defined genomic coordinates for each gene. The file must contain
a column named \code{gene_name} and strand information. These
coordinates are used to define gene-body regions, and transcription
start sites are derived from the coordinates according to strand.}

\item{num_of_promoter_tiles}{Integer. Number of promoter tiles to generate. Default: 20.}

\item{num_of_genebody_tiles}{Integer. Number of gene body tiles to generate. Default: 20.}

\item{num_of_intergenic_tiles}{Integer. Number of intergenic enhancer tiles to generate. Default: 1.}

\item{num_of_intragenic_tiles}{Integer. Number of intragenic enhancer tiles to generate. Default: 1.}

\item{fastMode}{Logical. If TRUE (default), runs an efficient model that excludes H3K27ac, intergenic enhancer, and intragenic enhancer sites.
If FALSE, these features are included, which may improve performance but increase runtime.}

\item{useAltPromoter}{Logical. Whether to use alternate promoters based on coverage. Default: TRUE.}

\item{verbose}{Logical. If TRUE, prints progress messages. Default: TRUE.}
}
\value{
A named numeric vector of predicted gene expression values.
}
\description{
This function extracts features from fragment files and generates (1) a dataframe
with several epigenetic features for each gene and (2) a numeric vector of predicted gene expression.
}
